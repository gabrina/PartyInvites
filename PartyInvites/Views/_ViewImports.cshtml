@*usage: to import all the files needed, in order to prevent repeatition
    it affects the current folders and subfolders
    the setting will be override, if the file comes in a subfolder folder ,instead of root*@


@using PartyInvites
@using PartyInvites.Models
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
